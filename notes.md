# 星之间 · 需求与蓝图

## 需求（用户 · 2026-08-29）

个人学习工作台，4 模块全开：考研备考 / 学期科目 / 待办 / 生活事项 + 今天要处理置顶。**身份边界：这是"学生的一面"**，不套开发者/AI 甲方流程；个性化处用户拍板。

## 维度体系（三方案）

| 方案 | 维度 | 形态 |
|---|---|---|
| A 便签墙（选定） | 空间 | 分块软木板墙一屏可见 + 作息侧栏 + 课表浮窗 + 便签盒 + 贴/撕/粘动画 |
| B 时间轴 | 时间 | 6:00-24:00 骨架，作息/课程固定块、任务可排入，现在线 |
| C 看板流 | 状态 | 四列（收件/今日/进行中/已完成）拖放流转 + 3D 翻面 + 自动计时 |

共性：localStorage 持久化、导出/导入 JSON、内联 SVG 零 emoji、桌面/移动双端、单向数据流（refreshAll 统一入口）。

## 材质重构历程（同一结构换皮）

1. 初版视觉（A/B/C 三稿）→ 用户选 B 琥珀夜灯。
2. 信息架构三版（书桌/旅程/星图）→ 判定「隐喻不即数据模型，不如不做」（教训）。
3. 方案 A 定稿结构 → 材质探索：
   - 纸品手账 / 工业仪表 / 织物软木（第一轮，评价"都一般"）
   - 花窗构成主义（用户库塔罗沉淀提示词）/ 至上主义（自研）/ **纯构成主义（选定）**
4. **用户拍板：纯构成主义·工程设计流**。

## 关键技术决策

- 交互不依赖 `prompt()`/`alert()`（iframe 预览环境会拦截）→ 全改内联输入框。
- 换皮 = 只动 `<style>` + 可选装饰，`<script>` 块与基座逐字符一致，保证功能永不走样。
- 便签 3:2 竖长方形（`aspect-ratio:2/3` + grid 多列）。

## 坑与教训

- 隐喻必须即数据模型（操作动词=增删改查），不是给 Tab 套皮。
- 视觉重构 ≠ 换色值；材质要真配方（feTurbulence/网格/剖线/箭头等），不能只换色。
- 子代理写文件可能带 CRLF，diff 前先归一化换行。
