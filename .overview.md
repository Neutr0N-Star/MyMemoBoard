# 星之间 · 学习工作台（便签墙）

> 一句话门牌：单文件 HTML 学习工作台，双击/打开 `index.html` 即用——四面便签墙（考研备考/学期科目/待办/生活事项）+ 日常作息 + 课程表 + 便签盒，贴/撕/粘动画，纯构成主义「工程设计流」视觉。

## 结构

- `index.html`：定稿版（构成主义·工程设计流）——4 面便签墙 + 作息侧栏 + 课程表浮窗 + 便签盒 + 贴/撕/粘动画 + 拖放换墙 + 导出/导入 + 移动端底部 Tab；localStorage 持久化；单文件全内联零外链
- `assets/archive/`：候选版本归档——纸品手账 / 工业仪表 / 织物软木 / 花窗构成主义 / 至上主义 / 方案A基座 / 方案B时间轴 / 方案C看板流（均可用，随时可换皮升级）
- `notes.md`：需求与蓝图（三版维度、材质重构历程、教训）
- 视觉风格卡：`01-Prompts\Art-Style\Constructivism\`（构成主义）+ `Suprematism\`（至上主义）——本项目的材质提示词沉淀

## 怎么用

1. 打开 `index.html`（浏览器直接拖入即可，无需服务器）。
2. 四区：考研备考 / 学期科目 / 待办 / 生活事项——点 `+` 贴便签，撕下=完成归档进便签盒，便签盒里可粘回/彻底丢掉；桌面可拖放换墙。
3. 侧边日常作息（点时间/内容可改）；课程表浮窗（点格子填课、+ 加课、点课程名改名/留空删除）。
4. 顶栏倒计时自动算到考研初试（默认 2026-12-19，改代码里 `settings.examDate`）。
5. 移动端：底部 Tab 切换墙面。
6. 导出/导入 JSON 备份数据。

> 技术：纯原生 JS（数据层→计算层→渲染层→行为层，refreshAll 统一入口），无任何框架/依赖/CDN；交互全部内联输入框（不依赖 prompt/alert 弹窗，预览环境可用）。
