# 星之间 · 发布前冒烟验收清单

> 打 tag 发版前，用这份清单快速过一遍。能过再发，别把坑留给用户。

## 1. 基础功能

- [ ] 双击 `release\XingZhiJian-1.0.0-portable.exe` 能打开独立窗口，无浏览器地址栏。
- [ ] 首次打开是空态引导，不遗留预置数据。
- [ ] 四面墙（考研/学期/待办/生活）+ 右上「+」能贴便签。
- [ ] 撕下便签进便签盒；便签盒可粘回 / 彻底丢掉。
- [ ] 拖放便签能换墙；拖墙头 `≡` 能换墙顺序。
- [ ] 双击墙名能改名；`×` 两次确认能删墙。
- [ ] 移动端底部 Tab 切换正常。

## 2. 课程表 / 作息

- [ ] 顶栏「课程表」进完整编辑页。
- [ ] 课程表格子是**扁格子**（宽 > 高）。
- [ ] `+ 加一节` / `− 删最后一节` 正常；删最后一节会弹确认框（确认框要显示在最上层）。
- [ ] `恢复默认节次` 点了立刻弹确认框，确认后回到 10 节。
- [ ] 点格子能选课程；侧栏时间轴会显示撞车段。

## 3. 桌面能力

- [ ] 顶栏「置顶」按钮只在 exe 里显示，点击能切换。
- [ ] 设置里「开机启动」能开/关，重启系统后生效。
- [ ] 设置里「通知」开/关正常；到点会弹系统通知（可手动把时间调到临近测试）。
- [ ] 数据出生在 exe 同目录 `data\`，不写 C 盘 AppData。

## 4. 数据与备份

- [ ] 导出 JSON 文件里 `settings.ai.key` 是 `***`，不是明文 Key。
- [ ] 导入导出的 JSON 能恢复数据，且不覆盖当前 API Key。
- [ ] 设置里开「自动备份」后，`data\backups\` 会出现 `backup-YYYY-MM-DD.json`。
- [ ] 导出 CSV 能在 Excel 打开，中文不乱码。
- [ ] 导出 Markdown 内容完整。

## 5. 安全与日志

- [ ] 控制台无 CSP 报错（F12 开 Developer Tools 看）。
- [ ] 故意触发一次页面错误，`data\logs\error.log` 有记录（桌面版）。
- [ ] 没有外链请求（除了你主动配置 AI 接口时）。

## 6. 安装包（如果发布 setup 版）

- [ ] `release\XingZhiJian-1.0.0-setup.exe` 能安装。
- [ ] 安装后开始菜单 / 桌面快捷方式存在。
- [ ] 卸载后不残留 `data\` 用户数据（portable 版才是数据随程序走）。

## 7. 打包一致性

- [ ] `release\XingZhiJian-1.0.0-portable.exe` 与 `SHA256SUMS.txt` 哈希一致。
- [ ] `app.asar` 内含 `preload.js`、`LICENSE`，且 `index.html` / `main.js` / `preload.js` 与源码一致。
