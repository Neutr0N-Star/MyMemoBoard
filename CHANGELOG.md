# Changelog

本项目不写重复系统日志，以 git 历史为准；本文件只做给用户看的版本要点。

## 1.2.0 (2026-08-31)

### 新增

- 字号调节：设置 → 字体 → 字号四档（小 13px / 标准 15px / 大 18px / 特大 21px）+ 自定义档（12~28px，超界自动钳制）
- UI 字号随正文小幅联动（标准档与旧版完全一致，默认零感知）：正文 13/15/18/21px 时 UI 13/15/17/19px，比例固定不乱
- 解决自定义字体导入后字形偏小、字显得小的问题——字号独立于字体可调

## 1.1.0 (2026-08-31)

### 修复（严重）

- **更新丢数据根因修复**：v1.0.x 数据目录是「绿色化」设计（存 exe 同目录 `data/`），自动更新会替换 exe 所在目录 → 更新后数据被覆盖丢失。现改为标准做法：数据存用户 `%APPDATA%\MyMemoBoards`，程序更新与数据彻底隔离，更新不再碰数据
- 数据迁移：旧版本 exe 同目录 `data/` 的数据不会自动搬移（避免误操作），如需保留请手动复制 `state.json` 到 `%APPDATA%\MyMemoBoards\` 下

## 1.0.4 (2026-08-31)

### 修复

- 恢复 App 内自动更新：1.0.3 改为浏览器打开发布页后确认内部下载链路可用且体验更好，现改回后台自动下载 + 下载完重启生效
- 下载不再无声挂起：新增下载进度条（实时百分比），网络连不到 GitHub 时明确提示「连接不到 GitHub」并给出可点击的发布页链接，手动下载兜底

### 优化

- 设置里「软件更新」说明改为「发现新版会在后台下载，下载完重启生效」

## 1.0.3 (2026-08-31)

### 修复

- 检查更新「弹了下就没反应」：此前发现新版后走 App 内后台下载（106MB 从 github.com 拉取），网络被拦时下载无声挂起、界面无任何反馈。现改为检测到新版直接弹出浏览器打开 GitHub Releases 下载页，由浏览器接管下载，不再依赖 App 内下载链路

### 优化

- 设置里「软件更新」说明同步改为「发现新版会在浏览器打开下载页，手动安装覆盖即可」

## 1.0.2 (2026-08-31)

### 新增

- 新增墙面类型：白板墙直接贴便签，分区墙支持创建、筛选、改名、删除子分区芯片
- 新增墙时可手动选择白板墙或分区墙；分区墙自动提供「未分类」子分区，删掉旧墙后仍可重新创建

### 优化

- 课程表从墙面按钮中剥离，快捷键移动到左侧日程表下方；课程表数据继续独立保留
- 默认墙名称由「考研备考」统一为「备考」，旧数据会自动迁移

## 1.0.1 (2026-08-31)

### 修复

- 「检查更新」假报新版：此前 `!!r` 判断导致检查成功即谎报有新版本；现按真实版本号比较，已是最新时如实提示
- 后台更新进度可见：发现新版 → 后台下载中 → 已就绪（重启生效），失败会提示
- 安装版快捷方式图标：此前 `signAndEditExecutable: false` 把图标资源写入也一并跳过了，现改用 `signExecutable: false`，恢复自定义图标

### 新增

- 字体设置（整个应用生效）：自选系统字体（微软雅黑 / 黑体 / 宋体 / 楷体 / 仿宋 / 等线），或导入本地 .ttf/.otf（桌面版，存 userData/fonts）

### 优化

- 便签文字加大加粗（15px / 800），墙上、便签盒、收纳箱统一

## 1.0.0 (2026-08-30)

### 新增

- 四面便签墙：考研备考 / 学期科目 / 待办 / 生活事项
- 便签贴 / 撕 / 粘动画，拖放换墙，便签盒与收纳箱
- 日常作息 + 完整课程表编辑（节次时间可增减、每节课起止可改）
- 考试倒计时（支持多个考试节点，里程碑变色）
- 现在指针 / 明天预览 / 每日任务 / AI 周报
- 数据快照（每天自动存，保留 7 天）
- Electron 桌面版：窗口置顶、系统通知、开机启动、单实例锁、数据随程序走

### 优化

- 课程表扁格子；课程表确认框层级修复
- 导出 JSON 将 API Key 脱敏为 `***`
- 空态引导；自绘确认框 / toast；无原生 `prompt` / `alert`
- 构成主义·工程设计流视觉，晷刻工作室 logo

### 发布 / 合规

- MIT LICENSE 随源码与发布包
- README 数据与隐私、许可证章节
- SHA256 校验和
- 安全加固：CSP、禁止页面跳转、权限请求全拒、全局错误日志
- 桌面版自动更新（基于 GitHub Releases，electron-updater）
- 文件化数据存储（state.json 原子写，data/ 随程序走）
- 安装包 / 卸载（NSIS 安装向导）
- 帮助 / 关于浮窗
- CI 自动构建 + 冒烟（GitHub Actions）

## 计划中（下一版）

- 代码签名（当前 `signExecutable: false`，未启用签名；图标与版本信息正常写入）
